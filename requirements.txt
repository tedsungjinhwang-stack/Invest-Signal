# 무인 크론 잡이라 정확히 고정 — 2026-07-22 GitHub Actions 실측 검증 조합.
# 올릴 때는 로컬에서 pytest + dry-run 확인 후 갱신할 것.
pandas==3.0.3
requests==2.34.2
PyYAML==6.0.3
yfinance==1.5.1
PySocks==1.7.1          # BINANCE_PROXY에 socks5:// 프록시를 쓸 때 필요
